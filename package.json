{
  "name": "btp-demo",
  "version": "1.0.0",
  "description": "A simple SAP CAP demo project.",
  "dependencies": {
    "@cap-js/sqlite": "^2.0.4",
    "@sap/cds": "^9",
    "morgan": "^1.10.1"
  },
  "devDependencies": {
    "@sap/cds-dk": "^9.4.3"
  },
  "scripts": {
    "start": "cds run --in-memory"
  },
  "cds": {
    "requires": {
      "db": {
        "kind": "sqlite",
        "model": [
          "db",
          "srv"
        ]
      }
    }
  }
}
